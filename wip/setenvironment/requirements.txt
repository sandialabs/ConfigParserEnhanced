# Core Requirements
# configparser (should be packaged with Python)

# Unit Testing Requirements
mock >= 4
pytest >= 6.2
pytest-cov >= 2.4

# Sandia Repositories
configparserenhanced @ git+ssh://git@internal.gitlab.server/trilinos-devops-consolidation/code/ConfigParserEnhanced.git@master#egg=configparserenhanced

